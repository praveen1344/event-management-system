import React,{Component} from 'react';

class EventFilter extends Component{
  constructor(props){
    super(props);
  }

  render(){
    return(
      <div>Add all the query fields here.</div>
    )
  }
}

export default EventFilter;
